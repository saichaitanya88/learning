describe("MaxPointsOnALine", () => {
    function Point(x, y) {
        this.x = x;
        this.y = y;
    }
    it("", () => {
        var maxPoints = function (points) {

        };
        //expect(maxPoints([[1, 1], [2, 2], [3, 3]])).toBe(3);
    });
});